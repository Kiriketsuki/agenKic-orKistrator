# Epic 3: T1 Merged, Feature/33 (T2/T3 LiteLLM + Providers) Ready

## Situation
Epic 3 (Model Gateway & Judge-Router) for agenKic-orKistrator. T1 (Gateway Interface & Types) has been fully reviewed via 3 adversarial councils, all advisory items resolved, squash-merged into epic/3, and the advisory fix cherry-picked on top. Feature/33 (T2/T3: LiteLLM Client & Provider Adapters) is rebased onto the updated epic/3 and ready for review/continuation.

## Current State
- Branch: `feature/33-feature-litellm-client-provider-adapters-t2-t3`
- Worktree: `/home/kiriketsuki/dev/Personal/agenKic-orKistrator/.claude/worktrees/epic-3-model-gateway`
- Uncommitted changes: none
- Tests: all passing (`internal/gateway/` and `internal/gateway/providers/`)
- PR #49 targets `epic/3-implement-model-gateway-judge-router`
- PR #36 targets `main` (tracking only)

### What's done
- T1 squash-merged into epic/3 (commit `2dd17b7`)
- Advisory fix cherry-picked onto epic/3 (commit `9a0d868`): `fmt.Formatter` on ProviderConfig, tightened test bounds, spec sync
- Feature/33 rebased onto updated epic/3 (clean, no conflicts)
- Issue #55 created: T7 CostReport aggregation test obligation
- Feature/33 already has 2 commits: T2/T3 implementation + spec

### What's incomplete
- Feature/33 code has NOT been reviewed by a council or PR review
- No council has run on the T2/T3 implementation
- The T2/T3 code was written in a prior session and may need updates now that T1 has the `Format()` method and `CacheHit` field

## Key Files
- `internal/gateway/gateway.go` -- T1 types and interfaces (authoritative)
- `internal/gateway/errors.go` -- sentinel errors and structured error types
- `internal/gateway/providers/provider.go` -- LiteLLM client, Completer wrapper
- `internal/gateway/providers/anthropic.go` -- Anthropic format adapter
- `internal/gateway/providers/openai.go` -- OpenAI format adapter
- `internal/gateway/providers/ollama.go` -- Ollama format adapter
- `internal/gateway/providers/provider_test.go` -- T2/T3 tests
- `litellm-client-provider-adapters-spec.md` -- T2/T3 feature spec
- `model-gateway-spec.md` -- epic-level spec with task breakdown

## Decisions Made
- `ProviderConfig.APIKey` kept exported with defense-in-depth (`json:"-"`, `String()` redaction, `fmt.Formatter`) rather than making it unexported (would break T2-T5 field access)
- `TokenUsage` type deliberately inlined -- `InputTokens`/`OutputTokens` carried directly on `CompletionResponse` and `CostRecord`
- `CacheHit bool` promoted to CostRecord must-have (council recommendation, for DeepSeek cached pricing)
- Council report files are session artifacts, not repo deliverables -- deleted from repo root
- `Router.Classify()` naming (not `Route()`) to avoid Go method-set ambiguity with `Gateway.Route()`
- `SystemPrompt` separate from `Messages[]` for Anthropic provider compatibility

## Failed Approaches
- None in this session. Prior councils (v1, v2, v3) are documented in commit messages.

## Active Constraints
- This is a git worktree at `.claude/worktrees/epic-3-model-gateway` -- do NOT cd to the original repo root
- Feature branches auto-created by `issue-branch-handler` workflow -- don't create branches manually
- All PRs are squash-merged per repo convention
- The epic has 5 feature branches: 32(T1), 33(T2/T3), 34(T4), 37(T5/T6), 38(T7)
- T2 obligation: `ProviderConfig.BaseURL` must validate host against allowlist and block private CIDRs (tracked as TODO(T2) in `gateway.go:181-182`)

## Next Steps
1. Read the T2/T3 implementation files in `internal/gateway/providers/` to understand current state
2. Read `litellm-client-provider-adapters-spec.md` for the feature spec
3. Review whether T2/T3 code needs updates for the T1 advisory changes (Format method, CacheHit field)
4. Run adversarial council on merging feature/33 into epic/3 (same pattern as T1)
5. Address any council conditions, commit, push, squash merge PR #49
6. Advance to feature/34 (Judge-Router, T4) or feature/37 (Config + Fallback, T5/T6)

## Open Questions
- Feature/33 code was written in a prior session -- needs freshness check against updated T1 types
- No feature spec file found named specifically for T2/T3 at expected path -- check `litellm-client-provider-adapters-spec.md`
